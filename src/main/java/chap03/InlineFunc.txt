inline function

1. 람다식을 사용했을 때 무의미한 객체 생성을 예방
  - java에선 객체를 호출할 때마다 새로이 만들어짐. 이런 문제점을 해결하기 위해서 inline 키워드 사용

2. 코드는 객체를 항상 새로 만드는 것이 아니라 해당 함수의 내용을 호출한 함수에 넣는 방식으로 컴파일 코드를 작성함

3. inline 키워드를 사용하면 그 함수를 호출하는 모든 문장을 함수 본문에 해당하는 바이트코드로 바꿔치기 한다.

4. 